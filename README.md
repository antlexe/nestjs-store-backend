# 🛒 Store Backend (NestJS + Prisma + PostgreSQL)

Тестовое задание для Backend-разработчика. Полнофункциональный API интернет-магазина с аутентификацией, ролями, товарами и заказами.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 22+
- PostgreSQL (локально или Neon.tech)
- npm или yarn

### Установка

```bash
git clone https://github.com/antlexe/nestjs-store-backend.git
cd nestjs-store-backend
npm install
```

### Настройка окружения

Значения из файла `.env.example` (**примеры для демонстрации**) перенесите в файл `.env`.

```bash
# Для продакшена замените на собственные:
DATABASE_URL — строка подключения к вашей PostgreSQL
JWT_SECRET — секретный ключ для JWT
```

### Миграции и запуск

```bash
npx prisma migrate dev
npm run start:dev
```

Приложение доступно на `http://localhost:3000`

## 📖 Swagger Документация

После запуска приложения доступна полная интерактивная документация API:

🌐 Swagger UI: `http://localhost:3000/docs`

## 📚 API Endpoints

### Auth

| Метод | Маршрут          | Описание                        |
| ----- | ---------------- | ------------------------------- |
| POST  | `/auth/register` | Регистрация нового пользователя |
| POST  | `/auth/login`    | Вход в систему                  |

### Products

| Метод  | Маршрут         | Доступ                                                          |
| ------ | --------------- | --------------------------------------------------------------- |
| GET    | `/products`     | Получить список товаров с пагинацией и фильтрацией по категории |
| GET    | `/products/:id` | Получить товар по ID                                            |
| POST   | `/products`     | Создание товара (только ADMIN)                                  |
| PATCH  | `/products/:id` | Обновить товар (только ADMIN)                                   |
| DELETE | `/products/:id` | Удалить товар (soft delete, только ADMIN)                       |

### Orders

| Метод | Маршрут       | Описание                                          |
| ----- | ------------- | ------------------------------------------------- |
| POST  | `/orders`     | Создать новый заказ                               |
| GET   | `/orders`     | Получить список заказов пользователя с пагинацией |
| GET   | `/orders/:id` | Получить детали заказа по ID                      |

## 🏗️ Архитектура

```bash
src/
├── auth/
│   ├── decorators/
│   ├── dto/
│   ├── guards/
│   ├── strategies/
│   ├── types/
│   ├── auth.controller.ts
│   ├── auth.service.ts
│   └── auth.module.ts
├── database/
│   ├── database.service.ts
│   └── database.module.ts
├── orders/
│   ├── dto/
│   ├── types/
│   ├── orders.controller.ts
│   ├── orders.service.ts
│   └── orders.module.ts
├── products/
│   ├── dto/
│   ├── products.controller.ts
│   ├── products.service.ts
│   └── products.module.ts
└── main.ts
```

## 🛠️ Технологии

- `NestJS` — прогрессивный Node.js фреймворк

- `TypeScript` — строгая типизация

- `PostgreSQL` ([Neon.tech](https://neon.tech)) — реляционная БД

- `Prisma` — ORM с типизацией и миграциями

- `Passport.js` — аутентификация (Local + JWT)

- `bcrypt` — хеширование паролей

- `class-validator` + `class-transformer` — валидация DTO

## 🔐 Безопасность

- JWT токены с expiresIn

- Хеширование паролей через bcrypt

- Guard'ы для защиты эндпоинтов

- Ролевая модель (USER / ADMIN)

- Soft delete для товаро

## 📦 Опциональные фичи (реализованы)

- ✅ Пагинация товаров и заказов

- ✅ Фильтрация товаров по категории (?category=electronics)

- ✅ Мягкое удаление товаров (isActive: false)

- ✅ Транзакции при создании заказа

- ✅ Проверка остатков товара

- ✅ Полная Swagger документация API

- ✅ Rate limiting (**10 запросов/минуту**) — защита от злоупотреблений

## 🔧 Разработка

```bash
# Генерация Prisma клиента
npx prisma generate

# Просмотр БД через Prisma Studio
npx prisma studio

# Продакшен сборка
npm run build
npm run start:prod
```

## Rate Limiting

Приложение защищено от злоупотреблений с помощью встроенного rate limiting:

- Лимит: 10 запросов в минуту на IP-адрес

- Статус при превышении: 429 Too Many Requests

- Область применения: Все эндпоинты API

- Цель: Защита от DoS-атак и brute force

## 👥 Collaborators

Проект разработан в рамках тестового задания. Добавлен в collaborators: [antonzhukouski](https://github.com/antonzhukouski).
